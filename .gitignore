# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.py[cod]

# Build / tooling caches
dist/
# ...but the printer UI ships pre-built, so track the frontend bundle.
!frontend/dist/
build/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
coverage/
htmlcov/
.vite/

# Environment / secrets
.env
.env.*
!.env.example
!.env.*.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Curated hardware DB: the editable source is kept LOCAL only — the repo ships the compiled,
# read-only hardware.sqlite (built by scripts/build_hardware_db.py). Keep the source out of git.
backend/app/data/reference/hardware.json
