# Normalize line endings to LF for all text files (matters for shell scripts and
# config files deployed to the Linux printer host).
* text=auto eol=lf
*.svg text eol=lf

# Binary assets - never normalize line endings.
*.png binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary
