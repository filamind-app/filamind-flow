fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
httpx>=0.27,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
# Parses Klipper's Kconfig for the firmware config editor (Klipper ships its own
# copy under lib/kconfiglib, used in preference to this one at runtime).
kconfiglib>=14.1,<15.0
# Powers the input-shaper resonance math vendored from Klipper
# (app/vendor/klipper_shaper) for the Input Shaping widget.
numpy>=1.26,<3.0
# Renders Klipper's own macro template dialect ({ … } expressions + {% … %} control flow) so the
# Macro Designer can expand loops / conditionals offline in a sandbox, the way the printer would.
jinja2>=3.1,<4.0
